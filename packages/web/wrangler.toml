name = "solid-glass-web"
compatibility_date = "2026-03-12"
pages_build_output_dir = "dist"

[build]
command = "pnpm install && pnpm --filter solid-glass build && pnpm --filter @solid-glass/web build"
cwd = "../.."
